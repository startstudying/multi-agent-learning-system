# ACCEPT-20260611 前端角色工作台分离

## 验收结论

通过。

## 验收项

- `/` 显示学生工作台壳，业务侧栏包含学生学习语义，不包含教师审核队列或管理员系统健康入口。
- `/teacher/reviews` 显示教师工作台壳，业务侧栏包含资源审核队列，不包含学生学习项目或管理员系统健康入口。
- `/admin/operations` 显示管理员工作台壳，业务侧栏包含系统健康和模型供应商入口，不包含学生学习项目或教师审核队列。
- `/admin/model-providers` 保留管理员二级入口能力。
- 前端测试通过。
- 前端构建通过。

## 剩余风险

当前只是角色工作台壳分离，不等于前端权限控制；真实权限仍由后端认证和 RBAC 负责。

学生页内部仍有固定示例状态，应在后续任务中拆为真实用户/课程/知识库选择和数据加载状态。
